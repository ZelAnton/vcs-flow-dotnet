<!-- Describe what this PR changes and why. Link any related issues. -->

## Summary

## Checklist

- [ ] `dotnet build Vcs.Flow.slnx` passes (warnings are errors).
- [ ] `dotnet test Vcs.Flow.slnx` passes.
- [ ] `CHANGELOG.md` updated under `## [Unreleased]` (or this is a pure internal refactor with no user-visible effect).
- [ ] New/changed commands keep the Native-AOT publish clean (no new trim/AOT analyzer warnings).
- [ ] Docs/README updated if behaviour, usage, or a command's flags changed.
